apiVersion: v1alpha1
kind: KubeAuthenticationConfig
configuration:
  anonymous:
    enabled: true
    conditions:
      - path: /livez
      - path: /readyz
      - path: /healthz
  jwt:
    - issuer:
        url: https://keycloak.cloud.danmanners.com/realms/gmh
        audiences:
          - helo-k8s
      claimMappings:
        username:
          claim: preferred_username
          prefix: "oidc:/"
        groups:
          claim: groups
          prefix: "oidc:/"
