apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: &ns wander-winter
  namespace: argocd
spec:
  description: Educational environment for the Wander Winter Namespace
  roles:
    - name: admin
      description: "Admins can do anything for their project"
      groups:
        - *ns
      policies:
        - p, proj:wander-winter:admin, applications, *, *, allow
  clusterResourceBlacklist:
    - group: "*"
      kind: "*"
  clusterResourceWhitelist:
    - group: ""
      kind: "persistentvolumes"
  destinations:
    # Allow deployments to wander-winter and wander-winter-* namespaces
    - namespace: *ns
      server: https://kubernetes.default.svc
    # Disallow deployments to non-wander-winter namespaces
    - namespace: "!*"
      server: https://kubernetes.default.svc
  sourceNamespaces:
    - *ns
  namespaceResourceWhitelist:
    - group: "*"
      kind: "*"
  namespaceResourceBlacklist:
    - group: ""
      kind: "ResourceQuota"
    - group: "ceph.rook.io"
      kind: "*"
    - group: "argoproj.io"
      kind: "appprojects"
  sourceRepos:
    - "*"
