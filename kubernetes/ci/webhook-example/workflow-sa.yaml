---
# Service account with NO cluster access - used by workflow step pods.
# No RoleBinding or ClusterRoleBinding - pods run with zero RBAC permissions.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: workflow-no-access
  namespace: cicd
