---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: k8s-monitoring
  namespace: argocd
  labels:
    chartVersion: &chartVersion 4.5.2
spec:
  destination:
    namespace: monitoring
    server: https://kubernetes.default.svc
  project: default
  sources:
    - repoURL: &repo https://github.com/GoodMannersHosting/home-enterprise-labops.git
      targetRevision: &targetRevision main
      ref: values
    - repoURL: *repo
      targetRevision: *targetRevision
      path: kubernetes/core/k8s-monitoring
    - name: controller
      repoURL: ghcr.io/grafana/helm-charts
      chart: k8s-monitoring
      targetRevision: *chartVersion
      helm:
        valueFiles:
          - $values/kubernetes/core/k8s-monitoring/values.yaml
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
      - CreateNamespace=true
